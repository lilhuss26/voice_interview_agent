[pytest]
testpaths = tests
# The repo root has an __init__.py, which makes pytest's rootdir insertion climb
# past the repo. Without this, `import agent` / `import src` do not resolve.
pythonpath = .
addopts = -q
